[project]
name = "exec-email-scraper"
version = "0.1.0"
description = "Finds executive contact emails from company domains for non-profit sponsorship outreach"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "python-dotenv",
    "openpyxl",
]
